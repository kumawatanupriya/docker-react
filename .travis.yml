sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t kumawatanupriya/frontend -f Dockerfile.dev .

script:
  - docker run -e CI=true kumawatanupriya/docker-react npm run test
